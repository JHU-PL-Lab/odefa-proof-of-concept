# EXPECT-TYPECHECKS-AND-EVALUATES

# The identity function from the overview.

f = fun x -> ( # an identity function
        i = fun a -> (
                r2 = a
            );
        r = i x;
        n0 = r;
    );
x1 = {y};
z1 = f x1; # evaluates to {y}
x2 = {n};
z2 = f x2; # evaluates to {n}
